str = 'Strings'

print(str[0])
print(str[-2]) # we can also the string by negative indexes also

print(f"Length of string is : {len(str)}")

print(f"Accessing all characters from the string : {str[0 : len(str)]}") # Slicing the string according to the required indexes



str = "0123456789"
print(str[0 : len(str) : 3]) # 0369

